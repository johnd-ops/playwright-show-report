# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: suite/e2e/partner-mock/clinician-delegation.journey.spec.ts >> Partner-mock — Clinician per-clinician delegation >> selecting a clinician narrows the patient roster to that clinician
- Location: suite/e2e/partner-mock/clinician-delegation.journey.spec.ts:102:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: page.waitForURL: Test timeout of 30000ms exceeded.
=========================== logs ===========================
waiting for navigation until "load"
  navigated to "https://partner-api-demo-play-space.vercel.app/"
============================================================
```

# Page snapshot

```yaml
- generic [ref=e1]:
  - banner [ref=e2]:
    - generic [ref=e3]:
      - link "PlaySpace Partner Mock The ecosystem, from three points of view." [ref=e4] [cursor=pointer]:
        - /url: /
        - generic [ref=e5]: PlaySpace Partner Mock
        - generic [ref=e6]: The ecosystem, from three points of view.
      - generic [ref=e7]: Auth0 M2M (real OAuth 2.0)
  - main [ref=e8]:
    - generic [ref=e10]:
      - generic [ref=e11]:
        - heading "Three views of one ecosystem" [level=1] [ref=e12]
        - paragraph [ref=e13]: "Pick a point of view. Each persona operates against the same backing organization — Evergreen Behavioral Health Network — so you can follow one story end to end: the Partner provisions clinics, staff, and patients; the Clinic Admin oversees a site; the Clinician works a delegated, roster-scoped view. Every activity here runs live against the Partner API."
      - generic [ref=e14]:
        - button "Enter the Partner workspace" [ref=e15] [cursor=pointer]:
          - generic [ref=e18]: The integration partner operating the org
          - heading "Partner" [level=3] [ref=e19]
          - paragraph [ref=e20]: "Provision the organization end-to-end: stand up clinics, add clinical staff, and enroll patients — every action a live, scoped, audited Partner API call."
          - list [ref=e21]:
            - listitem [ref=e22]:
              - generic [ref=e24]: Holds the Full administrative access level
            - listitem [ref=e25]:
              - generic [ref=e27]: Provision clinics, practitioners & patients (live)
            - listitem [ref=e28]:
              - generic [ref=e30]: Org-wide directory across every clinic
            - listitem [ref=e31]:
              - generic [ref=e33]: Live /health connectivity to the Partner API
          - generic [ref=e34]: Enter workspace →
        - button "Enter the Clinic Admin workspace" [ref=e35] [cursor=pointer]:
          - generic [ref=e38]: The clinic leader overseeing one site
          - heading "Clinic Admin" [level=3] [ref=e39]
          - paragraph [ref=e40]: "Oversee a single clinic: see every clinical staff member, open any one of them to view their schedule, and review the clinic patient roster."
          - list [ref=e41]:
            - listitem [ref=e42]:
              - generic [ref=e44]: Org-wide token at Full administrative access
            - listitem [ref=e45]:
              - generic [ref=e47]: App-side clinic filter narrows reads to one site
            - listitem [ref=e48]:
              - generic [ref=e50]: All clinical staff + each schedule (live)
            - listitem [ref=e51]:
              - generic [ref=e53]: Clinic patient roster (live)
          - generic [ref=e54]: Enter workspace →
        - button "Enter the Clinician workspace" [active] [ref=e55] [cursor=pointer]:
          - generic [ref=e58]: The practitioner working within the clinic
          - heading "Clinician" [level=3] [ref=e59]
          - paragraph [ref=e60]: "Work a delegated, roster-scoped view: see only my patients, manage my appointments, and book a virtual session that mints a real video join link."
          - list [ref=e61]:
            - listitem [ref=e62]:
              - generic [ref=e64]: Delegated token at Patient management level
            - listitem [ref=e65]:
              - generic [ref=e67]: My patients — delegated roster scoping (live)
            - listitem [ref=e68]:
              - generic [ref=e70]: My appointments + book a session (live)
            - listitem [ref=e71]:
              - generic [ref=e73]: Virtual booking mints a real video link (live)
          - generic [ref=e74]: Enter workspace →
      - paragraph [ref=e75]: The Clinician's per-clinician PHI scoping is enforced server-side by the EHR's row-level security. Every live number is a real, scoped, audited Partner API call.
  - contentinfo [ref=e76]:
    - generic [ref=e77]:
      - generic [ref=e78]: "Auth: Auth0 M2M (real OAuth 2.0)"
      - generic [ref=e79]:
        - text: "API:"
        - code [ref=e80]: https://playspace-non-hipaa-git-feature-new-apis-for-2facde-play-space.vercel.app
  - alert [ref=e81]
```

# Test source

```ts
  1   | /**
  2   |  * Partner-mock demo — Clinician persona per-clinician delegation journey.
  3   |  *
  4   |  * Surface under test: `services/partner-mock` (the three-persona demo over
  5   |  * the PlaySpace Partner API), deployed at PARTNER_MOCK_BASE_URL (default the
  6   |  * Vercel app). Runs under the `partner-mock-e2e` project — NO EHR auth, NO
  7   |  * storageState (the mock holds the partner token server-side).
  8   |  *
  9   |  * What this proves: selecting a clinician in the Clinician persona's identity
  10  |  * picker mints a DELEGATED partner token, and the EHR's two-tier RLS narrows
  11  |  * GET /v1/partner/patients to ONLY that clinician's roster. We assert the
  12  |  * narrowing on rendered DOM (the mock fetches the Partner API server-side, so
  13  |  * the browser never sees /v1/partner/patients as a network request — the
  14  |  * roster size surfaces as the `clinician-stat-patients` count on "My day",
  15  |  * which reflects the full delegated `listPatients` length, not a paginated
  16  |  * page).
  17  |  *
  18  |  * SKIP semantics (this is the EXPECTED pre-deploy state — never force a pass):
  19  |  *   - The deployed build runs in auth0 mode; per-clinician delegation only
  20  |  *     works once an Auth0 Action is applied to the tenant AND
  21  |  *     PARTNER_MOCK_AUTH0_DELEGATION_ENABLED is set. Until then the picker is
  22  |  *     rendered `disabled` with an honest banner — we skip with that reason.
  23  |  *   - If the deployed mock is unreachable we skip (navigation error), rather
  24  |  *     than failing a deploy-dependent infra condition.
  25  |  *
  26  |  * Live exploration (2026-06-30, deployed mock): delegation IS live — the
  27  |  * picker is enabled; org "Lighthouse Behavioral Health"; clinicians "Cleo
  28  |  * Clinician" (3 patients), "Caleb Clinician" (0 patients), "Aaron Admin". The
  29  |  * canonical PLAN expectation (Cleo 3 / Caleb 0) holds, so this spec asserts a
  30  |  * STRICT narrowing relationship between two clinicians and additionally pins
  31  |  * the canonical counts WHEN those exact clinician names are present — falling
  32  |  * back to a robust narrowing assertion if the seed data ever changes.
  33  |  *
  34  |  * No PHI: clinician (practitioner) names are not PHI; patient names are never
  35  |  * read or asserted — only roster COUNTS and partner-namespace UUIDs.
  36  |  */
  37  | 
  38  | import { test, expect, type Page } from '@playwright/test';
  39  | 
  40  | // Canonical deployed-data expectation from the delegation PLAN. Used only as
  41  | // an additional exact-count assertion when these clinician names are present;
  42  | // the spec's primary assertion is the seed-agnostic narrowing relationship.
  43  | const CANONICAL = {
  44  |   highName: 'Cleo Clinician',
  45  |   highCount: 3,
  46  |   lowName: 'Caleb Clinician',
  47  |   lowCount: 0,
  48  | } as const;
  49  | 
  50  | /** Open the demo landing and click through into the Clinician workspace.
  51  |  * A direct GET of /clinician 307-redirects without the `pm_persona` cookie,
  52  |  * so we MUST click the landing tile (it sets the cookie via a server action,
  53  |  * then routes). Returns false if the mock is unreachable. */
  54  | async function enterClinicianWorkspace(page: Page): Promise<boolean> {
  55  |   try {
  56  |     await page.goto('/', { waitUntil: 'domcontentloaded' });
  57  |   } catch {
  58  |     return false;
  59  |   }
  60  |   await expect(page.getByTestId('persona-tiles')).toBeVisible();
  61  |   await page.getByTestId('persona-tile-clinician').click();
> 62  |   await page.waitForURL(/\/clinician(\/|$|\?)/);
      |              ^ Error: page.waitForURL: Test timeout of 30000ms exceeded.
  63  |   return true;
  64  | }
  65  | 
  66  | /** Read the delegated roster size from the "My day" stat tile. This is the
  67  |  * authoritative roster count (full `listPatients` length, not a page). */
  68  | async function readRosterCount(page: Page): Promise<number> {
  69  |   const stat = page.getByTestId('clinician-stat-patients');
  70  |   await expect(stat).toBeVisible();
  71  |   const text = (await stat.textContent()) ?? '';
  72  |   // The tile reads e.g. "My patients3"; the trailing digits are the count.
  73  |   const match = text.match(/(\d+)\s*$/);
  74  |   expect(match, `could not parse roster count from "${text}"`).not.toBeNull();
  75  |   return Number(match![1]);
  76  | }
  77  | 
  78  | /** Select a clinician by their picker option testid and wait for the
  79  |  * server-action re-render to settle on THIS clinician's "My day".
  80  |  *
  81  |  * Critical: selecting a clinician triggers a `setClinicianIdentity` server
  82  |  * action → a Next server round-trip → a re-render. The roster stat tile is
  83  |  * ALREADY visible from the previous selection, so waiting on its visibility
  84  |  * alone races the revalidation and reads a stale count. We instead wait for
  85  |  * the "Working as <name>" header (server-rendered to match the selected
  86  |  * clinician) to confirm the round-trip for THIS selection has landed before
  87  |  * any count is read. `label` is the clinician's name (not PHI). */
  88  | async function selectClinician(
  89  |   page: Page,
  90  |   optionTestId: string,
  91  |   label: string,
  92  | ): Promise<void> {
  93  |   await page.getByTestId('clinician-identity-picker').click();
  94  |   await page.getByTestId(optionTestId).click();
  95  |   // Server round-trip landed for THIS clinician: the header names them, and
  96  |   // the roster stat tile is present.
  97  |   await expect(page.getByText(`Working as ${label}`)).toBeVisible();
  98  |   await expect(page.getByTestId('clinician-stat-patients')).toBeVisible();
  99  | }
  100 | 
  101 | test.describe('Partner-mock — Clinician per-clinician delegation', () => {
  102 |   test('selecting a clinician narrows the patient roster to that clinician', async ({
  103 |     page,
  104 |   }) => {
  105 |     const reachable = await enterClinicianWorkspace(page);
  106 |     test.skip(
  107 |       !reachable,
  108 |       'Deployed partner-mock is unreachable at PARTNER_MOCK_BASE_URL — skipping ' +
  109 |         '(infra/deploy condition, not a behaviour failure).',
  110 |     );
  111 | 
  112 |     const picker = page.getByTestId('clinician-identity-picker');
  113 | 
  114 |     // The headline gate: on the deployed auth0 build, delegation only works
  115 |     // once the Auth0 Action is applied AND PARTNER_MOCK_AUTH0_DELEGATION_ENABLED
  116 |     // is set. Until then the picker is either rendered `disabled` OR not
  117 |     // rendered at all (the deployed staging build omits it entirely). BOTH are
  118 |     // the CORRECT pre-deploy state — skip rather than hard-fail. The spec
  119 |     // greens once the operator re-deploys the delegated-token Auth0 Action and
  120 |     // sets the flag.
  121 |     const pickerVisible = await picker
  122 |       .waitFor({ state: 'visible', timeout: 10_000 })
  123 |       .then(() => true)
  124 |       .catch(() => false);
  125 |     test.skip(
  126 |       !pickerVisible,
  127 |       'Per-clinician delegation is NOT enabled on the deployed partner-mock yet ' +
  128 |         '(clinician identity picker is absent). Expected pre-deploy state until ' +
  129 |         'the operator applies the delegated-token Auth0 Action and sets ' +
  130 |         'PARTNER_MOCK_AUTH0_DELEGATION_ENABLED.',
  131 |     );
  132 | 
  133 |     const isDisabled = await picker.isDisabled();
  134 |     test.skip(
  135 |       isDisabled,
  136 |       'Per-clinician delegation is NOT enabled on the deployed partner-mock yet ' +
  137 |         '(clinician identity picker is disabled). This is the expected pre-deploy ' +
  138 |         'state for the auth0-mode build: the demo falls back to admin-wide reads ' +
  139 |         'until the operator applies the delegated-token Auth0 Action to the tenant ' +
  140 |         'and sets PARTNER_MOCK_AUTH0_DELEGATION_ENABLED. The spec will assert the ' +
  141 |         'narrowing once delegation is live.',
  142 |     );
  143 | 
  144 |     // --- Baseline: no identity chosen = the persona's unfiltered/no-scope view.
  145 |     // The Clinician home deliberately fetches NO patients until an identity is
  146 |     // chosen (an org-wide read would tell the wrong story), so the baseline is
  147 |     // the explicit "choose a clinician" prompt with no roster surfaced.
  148 |     await expect(page.getByTestId('clinician-no-identity')).toBeVisible();
  149 |     await expect(page.getByTestId('clinician-stat-patients')).toHaveCount(0);
  150 | 
  151 |     // Enumerate the real clinician options (testid = clinician-identity-<uuid>,
  152 |     // excluding the "none"/admin placeholder). Practitioner names are not PHI.
  153 |     // The picker is collapsed at first render, so its options aren't in the DOM
  154 |     // yet. Open it to enumerate, then close without selecting.
  155 |     await picker.click();
  156 |     const options = await page
  157 |       .locator('[role="option"][data-testid^="clinician-identity-"]')
  158 |       .evaluateAll((els) =>
  159 |         els
  160 |           .map((el) => ({
  161 |             testid: el.getAttribute('data-testid') ?? '',
  162 |             label: (el.textContent ?? '').trim(),
```