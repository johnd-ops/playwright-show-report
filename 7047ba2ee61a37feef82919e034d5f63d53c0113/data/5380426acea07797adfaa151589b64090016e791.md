# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: suite/e2e/alt-user/add-client-multiple-caregivers.alt-user.spec.ts >> Add Client with multiple caregivers — wizard + persistence + render >> two caregivers save successfully via the multi-step wizard
- Location: suite/e2e/alt-user/add-client-multiple-caregivers.alt-user.spec.ts:75:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: /^general$/i, level: 2 })
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('heading', { name: /^general$/i, level: 2 })

```

# Page snapshot

```yaml
- main [ref=e3]:
  - generic [ref=e8]:
    - generic [ref=e9]:
      - img "Playspace" [ref=e10]
      - heading "Playspace" [level=1] [ref=e11]
      - paragraph [ref=e13]: Log in to Playspace
    - generic [ref=e14]:
      - generic [ref=e15]:
        - button: Continue
        - generic [ref=e18]:
          - generic [ref=e20]:
            - generic [ref=e21]: Email address *
            - textbox "Email address" [active] [ref=e22]
          - generic [ref=e24]:
            - generic [ref=e25]: Password *
            - textbox "Password" [ref=e26]
            - switch "Show password" [ref=e27] [cursor=pointer]
        - paragraph [ref=e28]:
          - link "Reset password" [ref=e29] [cursor=pointer]:
            - /url: /u/login/password-reset-start/Username-Password-Authentication?state=hKFo2SBsWUdDVUlBdHprVFJlZk9HS2ZpV29SMVZPMDhCSHdrSKFur3VuaXZlcnNhbC1sb2dpbqN0aWTZIGJaZS14TjNNYXNvU0R2dWtSLVgxa1ozN19hV2FxeWJNo2NpZNkgVDR6bXFtMWs3WmpEM1pmdzJCbHNjdkZBNGtmS2NIZWk
        - button "Continue" [ref=e31] [cursor=pointer]
      - paragraph [ref=e33]:
        - text: Don't have an account?
        - link "Sign up" [ref=e34] [cursor=pointer]:
          - /url: /u/signup?state=hKFo2SBsWUdDVUlBdHprVFJlZk9HS2ZpV29SMVZPMDhCSHdrSKFur3VuaXZlcnNhbC1sb2dpbqN0aWTZIGJaZS14TjNNYXNvU0R2dWtSLVgxa1ozN19hV2FxeWJNo2NpZNkgVDR6bXFtMWs3WmpEM1pmdzJCbHNjdkZBNGtmS2NIZWk
      - generic [ref=e36]: Or
      - generic [ref=e37]:
        - button "Continue with Google" [ref=e39] [cursor=pointer]:
          - generic [ref=e41]: Continue with Google
        - button "Continue with Microsoft Account" [ref=e43] [cursor=pointer]:
          - generic [ref=e45]: Continue with Microsoft Account
```

# Test source

```ts
  36  | test.describe.configure({ mode: 'serial' });
  37  | 
  38  | type ClientCleanup = { id: string; label: string };
  39  | 
  40  | test.describe('Add Client with multiple caregivers — wizard + persistence + render', () => {
  41  |   let toCleanup: ClientCleanup[] = [];
  42  | 
  43  |   test.afterEach(async ({ request }) => {
  44  |     for (const c of toCleanup) {
  45  |       for (const url of [`/api/clients/${c.id}`, `/api/client/${c.id}`]) {
  46  |         try {
  47  |           const res = await request.delete(url);
  48  |           if (res.ok()) break;
  49  |         } catch {
  50  |           // try next form
  51  |         }
  52  |       }
  53  |     }
  54  |     toCleanup = [];
  55  |   });
  56  | 
  57  |   async function pickFirstDate(page: Page) {
  58  |     await page.getByTestId('date-of-birth-input').click();
  59  |     await page.waitForTimeout(500);
  60  |     const popover = page
  61  |       .locator('[role="dialog"], [role="grid"], [data-radix-popper-content-wrapper]')
  62  |       .last();
  63  |     const dayCell = popover
  64  |       .getByRole('button')
  65  |       .filter({ hasText: /^\d{1,2}$/ })
  66  |       .first();
  67  |     await dayCell.click();
  68  |   }
  69  | 
  70  |   async function pickFirstOption(page: Page, triggerTestId: string) {
  71  |     await page.getByTestId(triggerTestId).click();
  72  |     await page.getByRole('option').first().click();
  73  |   }
  74  | 
  75  |   test('two caregivers save successfully via the multi-step wizard', async ({
  76  |     page,
  77  |     request,
  78  |   }) => {
  79  |     test.fail(
  80  |       true,
  81  |       'BACKEND: POST /api/client returns 500 on dev with caregiver payload (Risk Assessment #46, Group A ticket #1). This multi-caregiver spec inherits the same blocker as the single-caregiver one. Test runs and fails-as-expected; when backend ships, test.fail flags unexpected-pass → remove marker.',
  82  |     );
  83  | 
  84  |     const stamp = Date.now();
  85  |     const clientFirst = `E2E-MULTI-CG-${stamp}`;
  86  |     const clientLast = 'TestClient';
  87  |     const clientEmail = `e2e-multi-cg-${stamp}@yopmail.com`;
  88  | 
  89  |     // Two distinct caregivers — different first names, different emails.
  90  |     const cg1 = {
  91  |       first: `E2E-CAREGIVER1-${stamp}`,
  92  |       last: 'TestCaregiverOne',
  93  |       email: `e2e-caregiver1-${stamp}@yopmail.com`,
  94  |       phone: '5550100002',
  95  |     };
  96  |     const cg2 = {
  97  |       first: `E2E-CAREGIVER2-${stamp}`,
  98  |       last: 'TestCaregiverTwo',
  99  |       email: `e2e-caregiver2-${stamp}@yopmail.com`,
  100 |       phone: '5550100003',
  101 |     };
  102 | 
  103 |     // Harvest ids from any POST response that returns them — so all
  104 |     // client + caregiver rows go to cleanup, regardless of how the
  105 |     // backend nests the response shape.
  106 |     function harvestIds(body: unknown, label: string) {
  107 |       const seen = new Set<string>();
  108 |       const visit = (val: unknown) => {
  109 |         if (!val || typeof val !== 'object') return;
  110 |         const v = val as Record<string, unknown>;
  111 |         if (typeof v.id === 'string' && /^[a-f0-9-]{36}$/.test(v.id)) {
  112 |           seen.add(v.id);
  113 |         }
  114 |         if (Array.isArray(val)) val.forEach(visit);
  115 |         else for (const k of Object.keys(v)) visit(v[k]);
  116 |       };
  117 |       visit(body);
  118 |       for (const id of seen) toCleanup.push({ id, label });
  119 |     }
  120 | 
  121 |     page.on('response', async (res) => {
  122 |       if (res.request().method() !== 'POST') return;
  123 |       if (!/\/api\/client(\b|\/|\?)/.test(res.url())) return;
  124 |       try {
  125 |         const body = await res.json();
  126 |         harvestIds(body, `POST ${res.url()}`);
  127 |       } catch {
  128 |         // non-JSON or empty body — nothing to harvest
  129 |       }
  130 |     });
  131 | 
  132 |     // ---------- Step 1: Client Info ----------
  133 |     await page.goto('/dashboard/clients/creation');
  134 |     await expect(
  135 |       page.getByRole('heading', { level: 2, name: /^general$/i }),
> 136 |     ).toBeVisible();
      |       ^ Error: expect(locator).toBeVisible() failed
  137 | 
  138 |     await page.getByTestId('first-name-input').fill(clientFirst);
  139 |     await page.getByTestId('last-name-input').fill(clientLast);
  140 |     await page.getByTestId('email-input').fill(clientEmail);
  141 |     await page.getByTestId('phone-number-input').fill('5550100001');
  142 |     await pickFirstOption(page, 'gender-select');
  143 |     await pickFirstDate(page);
  144 |     // Explicit therapist selection. Default user has therapist auto-
  145 |     // populated; alt user does not — the "Please select a therapist"
  146 |     // validation blocks Next otherwise. Picking the first option keeps
  147 |     // the spec robust across account states.
  148 |     await pickFirstOption(page, 'therapist-select');
  149 | 
  150 |     const next = page.getByTestId('next-step-button');
  151 |     await expect(next).toBeEnabled();
  152 |     await next.click();
  153 | 
  154 |     // ---------- Step 2: Caregiver Settings — TWO caregivers ----------
  155 |     await expect(
  156 |       page.getByText(/caregiver settings|caregiver information/i).first(),
  157 |       'step 2 (Caregiver) header should appear after Next from step 1',
  158 |     ).toBeVisible({ timeout: 10_000 });
  159 | 
  160 |     await expect(page.getByTestId('requires-caregiver-switch')).toBeVisible();
  161 |     await page.getByTestId('requires-caregiver-switch').click();
  162 |     await expect(page.getByTestId('caregiver-0-card')).toBeVisible();
  163 | 
  164 |     // Fill caregiver #0
  165 |     await page.getByTestId('caregiver-0-mode-create').click();
  166 |     await expect(page.getByTestId('caregiver-0-first-name-input')).toBeVisible();
  167 |     await page.getByTestId('caregiver-0-first-name-input').fill(cg1.first);
  168 |     await page.getByTestId('caregiver-0-last-name-input').fill(cg1.last);
  169 |     await page.getByTestId('caregiver-0-email-input').fill(cg1.email);
  170 |     await page.getByTestId('caregiver-0-phone-input').fill(cg1.phone);
  171 | 
  172 |     // Add the second caregiver. Probe canonical testid candidates and
  173 |     // fall back to a role-based "Add" / "+ Caregiver" button. Annotate
  174 |     // and skip cleanly if no add-affordance is reachable.
  175 |     const addCandidate = page
  176 |       .getByTestId('add-caregiver-button')
  177 |       .or(page.getByTestId('caregiver-add-button'))
  178 |       .or(page.getByRole('button', { name: /add\s*(another|second|\+)?\s*caregiver/i }))
  179 |       .or(page.getByRole('button', { name: /^\+\s*caregiver$/i }))
  180 |       .first();
  181 | 
  182 |     const addVisible = await addCandidate
  183 |       .isVisible({ timeout: 5_000 })
  184 |       .catch(() => false);
  185 | 
  186 |     if (!addVisible) {
  187 |       test.info().annotations.push({
  188 |         type: 'product-issue',
  189 |         description:
  190 |           'No "Add caregiver" affordance found on the creation wizard caregiver step. Multi-caregiver flow may not be supported yet, or the testid differs from the probed candidates (add-caregiver-button, caregiver-add-button). File a product ticket: pediatric clients commonly have two guardians; the wizard should support multiple caregivers.',
  191 |       });
  192 |       test.skip(
  193 |         true,
  194 |         'no multi-caregiver add affordance on the wizard (filed as a product issue)',
  195 |       );
  196 |       return;
  197 |     }
  198 | 
  199 |     await addCandidate.click();
  200 |     await expect(
  201 |       page.getByTestId('caregiver-1-card'),
  202 |       'red flag: clicked add-caregiver but caregiver-1-card never rendered',
  203 |     ).toBeVisible({ timeout: 5_000 });
  204 | 
  205 |     await page.getByTestId('caregiver-1-mode-create').click();
  206 |     await expect(page.getByTestId('caregiver-1-first-name-input')).toBeVisible();
  207 |     await page.getByTestId('caregiver-1-first-name-input').fill(cg2.first);
  208 |     await page.getByTestId('caregiver-1-last-name-input').fill(cg2.last);
  209 |     await page.getByTestId('caregiver-1-email-input').fill(cg2.email);
  210 |     await page.getByTestId('caregiver-1-phone-input').fill(cg2.phone);
  211 | 
  212 |     await page.getByTestId('next-step-button').click();
  213 | 
  214 |     // ---------- Step 3: Review & Submit ----------
  215 |     await expect(page.getByTestId('review-step')).toBeVisible();
  216 | 
  217 |     // The review step should ideally show BOTH caregivers. Best-effort
  218 |     // assertion — if the review surface doesn't enumerate them, the
  219 |     // create POST is still the load-bearing check below.
  220 |     const reviewText = ((await page.textContent('body')) ?? '').toLowerCase();
  221 |     if (!reviewText.includes(cg1.first.toLowerCase()) || !reviewText.includes(cg2.first.toLowerCase())) {
  222 |       test.info().annotations.push({
  223 |         type: 'product-issue',
  224 |         description: `Review step did not list both caregivers by name. Found cg1=${reviewText.includes(cg1.first.toLowerCase())}, cg2=${reviewText.includes(cg2.first.toLowerCase())}. Not blocking — the POST below is the authoritative check.`,
  225 |       });
  226 |     }
  227 | 
  228 |     await expect(page.getByTestId('submit-client-button')).toBeVisible();
  229 | 
  230 |     const createResponsePromise = page.waitForResponse(
  231 |       (r) =>
  232 |         r.request().method() === 'POST' &&
  233 |         /\/api\/client(\b|\/|\?)/.test(r.url()),
  234 |       { timeout: 30_000 },
  235 |     );
  236 |     await page.getByTestId('submit-client-button').click();
```