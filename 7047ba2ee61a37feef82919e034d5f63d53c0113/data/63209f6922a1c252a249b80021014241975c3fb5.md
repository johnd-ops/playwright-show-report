# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: suite/e2e/partner-mock/clinician-delegation.journey.spec.ts >> Partner-mock — Clinician per-clinician delegation >> selecting a clinician narrows the patient roster to that clinician
- Location: suite/e2e/partner-mock/clinician-delegation.journey.spec.ts:102:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByTestId('clinician-identity-picker')
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByTestId('clinician-identity-picker')

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - banner [ref=e2]:
    - generic [ref=e3]:
      - link "PlaySpace Partner Mock The ecosystem, from three points of view." [ref=e4] [cursor=pointer]:
        - /url: /
        - generic [ref=e5]: PlaySpace Partner Mock
        - generic [ref=e6]: The ecosystem, from three points of view.
      - generic [ref=e7]: Auth0 M2M (real OAuth 2.0)
  - main [ref=e8]:
    - generic [ref=e10]:
      - generic [ref=e11]:
        - generic [ref=e12]:
          - generic [ref=e13]: Viewing as
          - button "Switch to the Partner workspace" [ref=e14] [cursor=pointer]: Partner
          - button "Switch to the Clinic Admin workspace" [ref=e15] [cursor=pointer]: Clinic Admin
          - button "Switch to the Clinician workspace" [ref=e16] [cursor=pointer]: Clinician
          - button "Return to the role chooser" [ref=e17] [cursor=pointer]: ← All roles
        - generic [ref=e18]:
          - generic [ref=e19]: Access level
          - button "What do the access levels mean?" [ref=e20] [cursor=pointer]:
            - img [ref=e21]
            - text: What do these mean?
          - button "Read-only" [ref=e24] [cursor=pointer]
          - button "Scheduling" [ref=e25] [cursor=pointer]
          - button "Patient management" [pressed] [ref=e26] [cursor=pointer]
          - button "Full administrative access" [ref=e27] [cursor=pointer]
      - generic [ref=e28]:
        - generic [ref=e29]:
          - generic [ref=e32]: The practitioner working within the clinic
          - heading "Clinician workspace" [level=2] [ref=e33]
        - navigation "Clinician sections" [ref=e34]:
          - link "My day" [ref=e35] [cursor=pointer]:
            - /url: /clinician
          - link "Appointments" [ref=e36] [cursor=pointer]:
            - /url: /clinician/appointments
          - link "My patients" [ref=e37] [cursor=pointer]:
            - /url: /clinician/patients
          - link "Guardrails" [ref=e38] [cursor=pointer]:
            - /url: /clinician/guardrails
            - text: Guardrails
      - generic [ref=e40]:
        - generic [ref=e41]:
          - generic [ref=e43]:
            - generic [ref=e44]: Evergreen Behavioral Health Network returned an error
            - generic [ref=e45]: Status 500
          - paragraph [ref=e47]: Failed to resolve partner organization
        - generic [ref=e48]:
          - generic [ref=e49]:
            - heading "My day API calls" [level=3] [ref=e50]
            - generic [ref=e51]:
              - button "Copy all calls to the clipboard" [ref=e52] [cursor=pointer]:
                - img [ref=e53]
                - text: Copy all
              - generic [ref=e56]: "min remaining: —"
              - generic [ref=e57]: "hr remaining: —"
          - paragraph [ref=e58]: These are the real Partner API requests this page just made — each row is method · path · HTTP status · request id · round-trip time. Use the copy button on a row (or “Copy all”) to grab a one-line summary.
          - generic [ref=e59]:
            - generic [ref=e60]: "Token scopes:"
            - generic [ref=e61]: practitioners:read
            - generic [ref=e62]: clinics:read
            - generic [ref=e63]: patients:read
            - generic [ref=e64]: patients:write
            - generic [ref=e65]: appointments:read
            - generic [ref=e66]: appointments:write
          - table [ref=e69]:
            - rowgroup [ref=e70]:
              - row "Method Path Status Request ID Time Copy" [ref=e71]:
                - columnheader "Method" [ref=e72]
                - columnheader "Path" [ref=e73]
                - columnheader "Status" [ref=e74]
                - columnheader "Request ID" [ref=e75]
                - columnheader "Time" [ref=e76]
                - columnheader "Copy" [ref=e77]
            - rowgroup [ref=e78]:
              - row "GET /v1/partner/practitioners?limit=3 500 40176309 9825 ms Copy this call to the clipboard" [ref=e79]:
                - cell "GET" [ref=e80]
                - cell "/v1/partner/practitioners?limit=3" [ref=e81]
                - cell "500" [ref=e82]:
                  - generic [ref=e83]: "500"
                - cell "40176309" [ref=e84]
                - cell "9825 ms" [ref=e85]
                - cell "Copy this call to the clipboard" [ref=e86]:
                  - button "Copy this call to the clipboard" [ref=e87] [cursor=pointer]:
                    - img [ref=e88]
  - contentinfo [ref=e91]:
    - generic [ref=e92]:
      - generic [ref=e93]: "Auth: Auth0 M2M (real OAuth 2.0)"
      - generic [ref=e94]:
        - text: "API:"
        - code [ref=e95]: https://playspace-non-hipaa-git-feature-new-apis-for-2facde-play-space.vercel.app
  - alert [ref=e96]
```

# Test source

```ts
  13  |  * the browser never sees /v1/partner/patients as a network request — the
  14  |  * roster size surfaces as the `clinician-stat-patients` count on "My day",
  15  |  * which reflects the full delegated `listPatients` length, not a paginated
  16  |  * page).
  17  |  *
  18  |  * SKIP semantics (this is the EXPECTED pre-deploy state — never force a pass):
  19  |  *   - The deployed build runs in auth0 mode; per-clinician delegation only
  20  |  *     works once an Auth0 Action is applied to the tenant AND
  21  |  *     PARTNER_MOCK_AUTH0_DELEGATION_ENABLED is set. Until then the picker is
  22  |  *     rendered `disabled` with an honest banner — we skip with that reason.
  23  |  *   - If the deployed mock is unreachable we skip (navigation error), rather
  24  |  *     than failing a deploy-dependent infra condition.
  25  |  *
  26  |  * Live exploration (2026-06-30, deployed mock): delegation IS live — the
  27  |  * picker is enabled; org "Lighthouse Behavioral Health"; clinicians "Cleo
  28  |  * Clinician" (3 patients), "Caleb Clinician" (0 patients), "Aaron Admin". The
  29  |  * canonical PLAN expectation (Cleo 3 / Caleb 0) holds, so this spec asserts a
  30  |  * STRICT narrowing relationship between two clinicians and additionally pins
  31  |  * the canonical counts WHEN those exact clinician names are present — falling
  32  |  * back to a robust narrowing assertion if the seed data ever changes.
  33  |  *
  34  |  * No PHI: clinician (practitioner) names are not PHI; patient names are never
  35  |  * read or asserted — only roster COUNTS and partner-namespace UUIDs.
  36  |  */
  37  | 
  38  | import { test, expect, type Page } from '@playwright/test';
  39  | 
  40  | // Canonical deployed-data expectation from the delegation PLAN. Used only as
  41  | // an additional exact-count assertion when these clinician names are present;
  42  | // the spec's primary assertion is the seed-agnostic narrowing relationship.
  43  | const CANONICAL = {
  44  |   highName: 'Cleo Clinician',
  45  |   highCount: 3,
  46  |   lowName: 'Caleb Clinician',
  47  |   lowCount: 0,
  48  | } as const;
  49  | 
  50  | /** Open the demo landing and click through into the Clinician workspace.
  51  |  * A direct GET of /clinician 307-redirects without the `pm_persona` cookie,
  52  |  * so we MUST click the landing tile (it sets the cookie via a server action,
  53  |  * then routes). Returns false if the mock is unreachable. */
  54  | async function enterClinicianWorkspace(page: Page): Promise<boolean> {
  55  |   try {
  56  |     await page.goto('/', { waitUntil: 'domcontentloaded' });
  57  |   } catch {
  58  |     return false;
  59  |   }
  60  |   await expect(page.getByTestId('persona-tiles')).toBeVisible();
  61  |   await page.getByTestId('persona-tile-clinician').click();
  62  |   await page.waitForURL(/\/clinician(\/|$|\?)/);
  63  |   return true;
  64  | }
  65  | 
  66  | /** Read the delegated roster size from the "My day" stat tile. This is the
  67  |  * authoritative roster count (full `listPatients` length, not a page). */
  68  | async function readRosterCount(page: Page): Promise<number> {
  69  |   const stat = page.getByTestId('clinician-stat-patients');
  70  |   await expect(stat).toBeVisible();
  71  |   const text = (await stat.textContent()) ?? '';
  72  |   // The tile reads e.g. "My patients3"; the trailing digits are the count.
  73  |   const match = text.match(/(\d+)\s*$/);
  74  |   expect(match, `could not parse roster count from "${text}"`).not.toBeNull();
  75  |   return Number(match![1]);
  76  | }
  77  | 
  78  | /** Select a clinician by their picker option testid and wait for the
  79  |  * server-action re-render to settle on THIS clinician's "My day".
  80  |  *
  81  |  * Critical: selecting a clinician triggers a `setClinicianIdentity` server
  82  |  * action → a Next server round-trip → a re-render. The roster stat tile is
  83  |  * ALREADY visible from the previous selection, so waiting on its visibility
  84  |  * alone races the revalidation and reads a stale count. We instead wait for
  85  |  * the "Working as <name>" header (server-rendered to match the selected
  86  |  * clinician) to confirm the round-trip for THIS selection has landed before
  87  |  * any count is read. `label` is the clinician's name (not PHI). */
  88  | async function selectClinician(
  89  |   page: Page,
  90  |   optionTestId: string,
  91  |   label: string,
  92  | ): Promise<void> {
  93  |   await page.getByTestId('clinician-identity-picker').click();
  94  |   await page.getByTestId(optionTestId).click();
  95  |   // Server round-trip landed for THIS clinician: the header names them, and
  96  |   // the roster stat tile is present.
  97  |   await expect(page.getByText(`Working as ${label}`)).toBeVisible();
  98  |   await expect(page.getByTestId('clinician-stat-patients')).toBeVisible();
  99  | }
  100 | 
  101 | test.describe('Partner-mock — Clinician per-clinician delegation', () => {
  102 |   test('selecting a clinician narrows the patient roster to that clinician', async ({
  103 |     page,
  104 |   }) => {
  105 |     const reachable = await enterClinicianWorkspace(page);
  106 |     test.skip(
  107 |       !reachable,
  108 |       'Deployed partner-mock is unreachable at PARTNER_MOCK_BASE_URL — skipping ' +
  109 |         '(infra/deploy condition, not a behaviour failure).',
  110 |     );
  111 | 
  112 |     const picker = page.getByTestId('clinician-identity-picker');
> 113 |     await expect(picker).toBeVisible();
      |                          ^ Error: expect(locator).toBeVisible() failed
  114 | 
  115 |     // The headline gate: on the deployed auth0 build, delegation only works
  116 |     // once the Auth0 Action is applied AND PARTNER_MOCK_AUTH0_DELEGATION_ENABLED
  117 |     // is set. Until then the picker is rendered `disabled`. Skipping here is
  118 |     // the CORRECT pre-deploy state — the spec greens once the operator
  119 |     // re-deploys the delegated-token Auth0 Action and sets the flag.
  120 |     const isDisabled = await picker.isDisabled();
  121 |     test.skip(
  122 |       isDisabled,
  123 |       'Per-clinician delegation is NOT enabled on the deployed partner-mock yet ' +
  124 |         '(clinician identity picker is disabled). This is the expected pre-deploy ' +
  125 |         'state for the auth0-mode build: the demo falls back to admin-wide reads ' +
  126 |         'until the operator applies the delegated-token Auth0 Action to the tenant ' +
  127 |         'and sets PARTNER_MOCK_AUTH0_DELEGATION_ENABLED. The spec will assert the ' +
  128 |         'narrowing once delegation is live.',
  129 |     );
  130 | 
  131 |     // --- Baseline: no identity chosen = the persona's unfiltered/no-scope view.
  132 |     // The Clinician home deliberately fetches NO patients until an identity is
  133 |     // chosen (an org-wide read would tell the wrong story), so the baseline is
  134 |     // the explicit "choose a clinician" prompt with no roster surfaced.
  135 |     await expect(page.getByTestId('clinician-no-identity')).toBeVisible();
  136 |     await expect(page.getByTestId('clinician-stat-patients')).toHaveCount(0);
  137 | 
  138 |     // Enumerate the real clinician options (testid = clinician-identity-<uuid>,
  139 |     // excluding the "none"/admin placeholder). Practitioner names are not PHI.
  140 |     // The picker is collapsed at first render, so its options aren't in the DOM
  141 |     // yet. Open it to enumerate, then close without selecting.
  142 |     await picker.click();
  143 |     const options = await page
  144 |       .locator('[role="option"][data-testid^="clinician-identity-"]')
  145 |       .evaluateAll((els) =>
  146 |         els
  147 |           .map((el) => ({
  148 |             testid: el.getAttribute('data-testid') ?? '',
  149 |             label: (el.textContent ?? '').trim(),
  150 |           }))
  151 |           .filter((o) => o.testid !== 'clinician-identity-none'),
  152 |       );
  153 |     await page.keyboard.press('Escape');
  154 | 
  155 |     expect(
  156 |       options.length,
  157 |       'expected at least two real clinician options to compare rosters',
  158 |     ).toBeGreaterThanOrEqual(2);
  159 | 
  160 |     // Measure every clinician's delegated roster size.
  161 |     const measured: { testid: string; label: string; count: number }[] = [];
  162 |     for (const opt of options) {
  163 |       await selectClinician(page, opt.testid, opt.label);
  164 |       const count = await readRosterCount(page);
  165 |       measured.push({ testid: opt.testid, label: opt.label, count });
  166 |     }
  167 | 
  168 |     // --- Primary assertion (seed-agnostic): the delegated token genuinely
  169 |     // SCOPES the roster per clinician — not every clinician sees the same set.
  170 |     // A non-delegated (admin-wide) token would make every roster identical, so
  171 |     // a difference between clinicians is the proof that delegation is working.
  172 |     const counts = measured.map((m) => m.count);
  173 |     const distinct = new Set(counts);
  174 |     const maxCount = Math.max(...counts);
  175 | 
  176 |     expect(
  177 |       maxCount,
  178 |       `at least one clinician should have a non-empty delegated roster; measured ${JSON.stringify(
  179 |         measured.map((m) => ({ label: m.label, count: m.count })),
  180 |       )}`,
  181 |     ).toBeGreaterThan(0);
  182 | 
  183 |     expect(
  184 |       distinct.size,
  185 |       `clinicians should NOT all see the same roster size under delegation — ` +
  186 |         `that would indicate admin-wide (unscoped) reads. measured ${JSON.stringify(
  187 |           measured.map((m) => ({ label: m.label, count: m.count })),
  188 |         )}`,
  189 |     ).toBeGreaterThan(1);
  190 | 
  191 |     // --- Canonical exact-count assertion (only when the PLAN's clinician names
  192 |     // are present). Pins the documented deployed data: Cleo=3, Caleb=0.
  193 |     const high = measured.find((m) => m.label === CANONICAL.highName);
  194 |     const low = measured.find((m) => m.label === CANONICAL.lowName);
  195 |     if (high && low) {
  196 |       expect(
  197 |         high.count,
  198 |         `${CANONICAL.highName} delegated roster size`,
  199 |       ).toBe(CANONICAL.highCount);
  200 |       expect(
  201 |         low.count,
  202 |         `${CANONICAL.lowName} delegated roster size`,
  203 |       ).toBe(CANONICAL.lowCount);
  204 |       // And the strict narrowing relationship between the two.
  205 |       expect(low.count).toBeLessThan(high.count);
  206 |     }
  207 | 
  208 |     // --- Roster-page corroboration: re-select the clinician with the largest
  209 |     // roster and confirm the dedicated patients page renders that clinician's
  210 |     // rows (a strict, non-empty set) rather than an org-wide list. We compare
  211 |     // against the SMALLEST-roster clinician to prove the page narrows too.
  212 |     const biggest = measured.reduce((a, b) => (b.count > a.count ? b : a));
  213 |     const smallest = measured.reduce((a, b) => (b.count < a.count ? b : a));
```