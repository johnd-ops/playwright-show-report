# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: suite/e2e/dashboard/playroom-edit.journey.spec.ts >> Playrooms — editor (design / publish / content) on a copy >> Content — optional content adds/removes; the mandatory one cannot be removed
- Location: suite/e2e/dashboard/playroom-edit.journey.spec.ts:143:7

# Error details

```
Test timeout of 90000ms exceeded.
```

```
Error: locator.isChecked: Test timeout of 90000ms exceeded.
Call log:
  - waiting for getByRole('heading', { name: 'Activity Shelf', exact: true, level: 4 }).locator('xpath=following::*[@role="switch"][1]')

```

# Test source

```ts
  1   | import { expect, type Locator, type Page } from '@playwright/test';
  2   | 
  3   | // Content / session-item switch kit — extracts the near-identical logic
  4   | // duplicated between the Playroom editor ("Content" tab) and the Toolkit
  5   | // editor ("Session Items"). Both render the same set of content types, each
  6   | // as a heading followed (in document order) by a role="switch" toggle, and
  7   | // both enforce a "keep at least one" rule (the last enabled toggle becomes
  8   | // disabled so the room/toolkit always has ≥1 content).
  9   | //
  10  | // Used by:
  11  | //   - playroom-edit.journey.spec.ts   (Content tab, <h4> headings)
  12  | //   - toolkit-crud.journey.spec.ts    (Session Items, any-level headings)
  13  | //
  14  | // The locator scopes by heading so it avoids the editor's separate publish
  15  | // switch and the Legacy Content Library cards (same names, but rendered as
  16  | // cards, not headings).
  17  | 
  18  | // The six content / session-item types shown in both editors, in display
  19  | // order.
  20  | export const CONTENT_SWITCH_TYPES = [
  21  |   'Activity Shelf',
  22  |   'Multiplayer Games',
  23  |   'Single Player Games',
  24  |   'Whiteboard',
  25  |   'Sand Tray',
  26  |   'Dollhouse',
  27  | ] as const;
  28  | 
  29  | /**
  30  |  * Locator for a single content/session-item toggle: the first role="switch"
  31  |  * that follows the type's heading in document order.
  32  |  *
  33  |  * `headingLevel` narrows to a specific heading level — the Playroom editor
  34  |  * uses <h4> rows, so pass `4` there to disambiguate. Omit it (Toolkit, whose
  35  |  * Session Items headings aren't a fixed level) to match any heading level and
  36  |  * take the first.
  37  |  */
  38  | export function contentSwitch(
  39  |   page: Page,
  40  |   name: string,
  41  |   headingLevel?: number,
  42  | ): Locator {
  43  |   const heading = headingLevel
  44  |     ? page.getByRole('heading', { name, level: headingLevel, exact: true })
  45  |     : page.getByRole('heading', { name, exact: true }).first();
  46  |   return heading.locator('xpath=following::*[@role="switch"][1]');
  47  | }
  48  | 
  49  | /**
  50  |  * Collect the content/session-item switches that are actually present, in
  51  |  * display order. Types absent from a given editor are skipped.
  52  |  */
  53  | export async function collectContentSwitches(
  54  |   page: Page,
  55  |   headingLevel?: number,
  56  | ): Promise<Locator[]> {
  57  |   const found: Locator[] = [];
  58  |   for (const t of CONTENT_SWITCH_TYPES) {
  59  |     const s = contentSwitch(page, t, headingLevel);
  60  |     if (await s.count()) found.push(s);
  61  |   }
  62  |   return found;
  63  | }
  64  | 
  65  | /** The subset of the given switches that are currently checked (on). */
  66  | export async function checkedSwitches(switches: Locator[]): Promise<Locator[]> {
  67  |   const on: Locator[] = [];
> 68  |   for (const s of switches) if (await s.isChecked()) on.push(s);
      |                                         ^ Error: locator.isChecked: Test timeout of 90000ms exceeded.
  69  |   return on;
  70  | }
  71  | 
  72  | /**
  73  |  * Drive + assert the shared "items can be toggled but at least one must
  74  |  * remain" rule used by both the Playroom (Content) and Toolkit (Session
  75  |  * Items) editors:
  76  |  *
  77  |  *   1. enable items until at least two are on
  78  |  *   2. disable down to exactly one remaining
  79  |  *   3. assert the last remaining toggle is DISABLED (the keep-≥1 rule is
  80  |  *      enforced so the editor can't be left with zero content)
  81  |  *
  82  |  * `label` only flavours the assertion messages ("content" vs "session").
  83  |  */
  84  | export async function assertKeepAtLeastOne(
  85  |   page: Page,
  86  |   switches: Locator[],
  87  |   label = 'content',
  88  | ): Promise<void> {
  89  |   expect(
  90  |     switches.length,
  91  |     `${label} items must list multiple items`,
  92  |   ).toBeGreaterThan(1);
  93  | 
  94  |   // ADD: enable items until at least two are on.
  95  |   for (const s of switches) {
  96  |     if ((await checkedSwitches(switches)).length >= 2) break;
  97  |     if (!(await s.isChecked()) && (await s.isEnabled())) {
  98  |       await s.click();
  99  |       await page.waitForTimeout(400);
  100 |     }
  101 |   }
  102 |   let on = await checkedSwitches(switches);
  103 |   expect(
  104 |     on.length,
  105 |     `must be able to enable at least two ${label} items`,
  106 |   ).toBeGreaterThanOrEqual(2);
  107 | 
  108 |   // REMOVE: disable down to exactly one remaining.
  109 |   for (let i = 0; i < on.length - 1; i++) {
  110 |     if (await on[i].isEnabled()) {
  111 |       await on[i].click();
  112 |       await page.waitForTimeout(400);
  113 |     }
  114 |   }
  115 |   on = await checkedSwitches(switches);
  116 |   expect(
  117 |     on.length,
  118 |     `${label} items must be reducible to one remaining`,
  119 |   ).toBe(1);
  120 | 
  121 |   // KEEP-AT-LEAST-ONE: the last remaining item must NOT be removable.
  122 |   await expect(
  123 |     on[0],
  124 |     `red flag: the last remaining ${label} item is still removable — the "keep at least one" rule is not enforced`,
  125 |   ).toBeDisabled();
  126 | }
  127 | 
```