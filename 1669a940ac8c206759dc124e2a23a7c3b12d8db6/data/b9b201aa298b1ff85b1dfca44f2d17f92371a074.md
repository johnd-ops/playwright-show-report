# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: suite/security/ai-rate-limit.spec.ts >> AI endpoints — rate limit probe (regression #20) >> forms/extract-ai produces at least one 429 under a 25-request burst
- Location: suite/security/ai-rate-limit.spec.ts:89:7

# Error details

```
Error: red flag: 25 parallel requests to /api/forms/extract-ai produced ZERO 429 (status mix: 400=25).

expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Test source

```ts
  13  | //   triggers the existing 10-char Zod min-length check → 400 early,
  14  | //   no actual LLM call). The status mix should include 429 (Too
  15  | //   Many Requests) after the configured threshold.
  16  | //
  17  | //   If we see 25 consecutive 4xx with no 429, the rate limiter is
  18  | //   not wired into this endpoint — finding #20 confirmed.
  19  | //
  20  | // Cost guard: every probe payload is deliberately invalid so no real
  21  | // LLM / n8n / ElevenLabs usage is charged. Even with 25 parallel
  22  | // requests the spend is zero — the goal is to count 429s in the
  23  | // response distribution, not to drive an actual generation.
  24  | //
  25  | // Subtle but important: the per-user rate limiter must fire BEFORE
  26  | // the input-validation step in the middleware chain. If it fires
  27  | // after, garbage payloads (like the ones below) bypass it entirely
  28  | // even when it exists — the attacker still wins the cost-budget
  29  | // counters, log volume, DB lookups, and auth checks. These probes
  30  | // deliberately use invalid payloads, so a "fixed" endpoint MUST
  31  | // still return ≥1 × 429 to pass this assertion.
  32  | //
  33  | // All five tests are wrapped in test.fail() today per the audit —
  34  | // currently no rate limiter exists, so we expect zero 429s. Remove
  35  | // the marker on each test once per-user rate limiting (Upstash/LRU)
  36  | // lands for that endpoint.
  37  | 
  38  | const BURST_COUNT = 25;
  39  | const PROBE_PROMPT = 'tiny'; // < 10 chars → 400 from Zod, no LLM call
  40  | const TIMEOUT_PER_REQ_MS = 60_000;
  41  | 
  42  | async function fireBurst(
  43  |   fire: () => Promise<APIResponse>,
  44  | ): Promise<Map<number, number>> {
  45  |   const results = await Promise.all(
  46  |     Array.from({ length: BURST_COUNT }, () => fire().catch((e) => e)),
  47  |   );
  48  |   const tally = new Map<number, number>();
  49  |   for (const r of results) {
  50  |     if (r && typeof r.status === 'function') {
  51  |       const s = r.status();
  52  |       tally.set(s, (tally.get(s) ?? 0) + 1);
  53  |     } else {
  54  |       tally.set(-1, (tally.get(-1) ?? 0) + 1);
  55  |     }
  56  |   }
  57  |   return tally;
  58  | }
  59  | 
  60  | test.describe('AI endpoints — rate limit probe (regression #20)', () => {
  61  |   test.describe.configure({ mode: 'serial' });
  62  | 
  63  |   test('forms/generate-ai produces at least one 429 under a 25-request burst', async ({
  64  |     request,
  65  |   }) => {
  66  |     test.fail(
  67  |       true,
  68  |       'Risk Assessment finding #20: no per-user rate limit on forms/generate-ai. This test currently fails because every burst request goes through. Remove test.fail once Upstash/LRU rate limiting lands.',
  69  |     );
  70  | 
  71  |     const tally = await fireBurst(() =>
  72  |       request.post('/api/forms/generate-ai', {
  73  |         data: { prompt: PROBE_PROMPT },
  74  |         timeout: TIMEOUT_PER_REQ_MS,
  75  |       }),
  76  |     );
  77  | 
  78  |     const statusSummary = Array.from(tally.entries())
  79  |       .sort(([a], [b]) => a - b)
  80  |       .map(([s, c]) => `${s}=${c}`)
  81  |       .join(', ');
  82  | 
  83  |     expect(
  84  |       (tally.get(429) ?? 0) > 0,
  85  |       `red flag: 25 parallel requests to /api/forms/generate-ai produced ZERO 429 responses (status mix: ${statusSummary}). Rate limiter is not wired in. An authenticated user can burst 1000+ paid LLM calls.`,
  86  |     ).toBe(true);
  87  |   });
  88  | 
  89  |   test('forms/extract-ai produces at least one 429 under a 25-request burst', async ({
  90  |     request,
  91  |   }) => {
  92  |     test.fail(
  93  |       true,
  94  |       'Risk Assessment finding #20: no rate limit on forms/extract-ai.',
  95  |     );
  96  | 
  97  |     // Empty multipart — early validation rejection, no n8n call.
  98  |     const tally = await fireBurst(() =>
  99  |       request.post('/api/forms/extract-ai', {
  100 |         multipart: {},
  101 |         timeout: TIMEOUT_PER_REQ_MS,
  102 |       }),
  103 |     );
  104 | 
  105 |     const statusSummary = Array.from(tally.entries())
  106 |       .sort(([a], [b]) => a - b)
  107 |       .map(([s, c]) => `${s}=${c}`)
  108 |       .join(', ');
  109 | 
  110 |     expect(
  111 |       (tally.get(429) ?? 0) > 0,
  112 |       `red flag: 25 parallel requests to /api/forms/extract-ai produced ZERO 429 (status mix: ${statusSummary}).`,
> 113 |     ).toBe(true);
      |       ^ Error: red flag: 25 parallel requests to /api/forms/extract-ai produced ZERO 429 (status mix: 400=25).
  114 |   });
  115 | 
  116 |   // ---------------------------------------------------------------------
  117 |   // Gap coverage added 2026-06-04 per QA on ENG-2433. The forms/
  118 |   // generate-ai + forms/extract-ai probes above cover the two endpoints
  119 |   // the ticket called out; these three cover the remaining AI surfaces
  120 |   // the ticket flagged as "same shape" — storybook generation, ai-note
  121 |   // transcription (the AI note-taker upload path), and clinical-note
  122 |   // auto-generation. All use early-rejection payloads so spend stays
  123 |   // zero regardless of burst size.
  124 |   // ---------------------------------------------------------------------
  125 | 
  126 |   test('storybooks/generate produces at least one 429 under a 25-request burst', async ({
  127 |     request,
  128 |   }) => {
  129 |     test.fail(
  130 |       true,
  131 |       'Risk Assessment finding #20 (gap coverage per ENG-2433): no rate limit on /api/storybooks/generate. Image + LLM generation per call — high cost-abuse vector. Remove test.fail once rate limiting lands.',
  132 |     );
  133 | 
  134 |     // Empty body — Zod validation rejects before any storybook /
  135 |     // image-generation work fires. No OpenAI / image-model spend.
  136 |     const tally = await fireBurst(() =>
  137 |       request.post('/api/storybooks/generate', {
  138 |         data: {},
  139 |         timeout: TIMEOUT_PER_REQ_MS,
  140 |       }),
  141 |     );
  142 | 
  143 |     const statusSummary = Array.from(tally.entries())
  144 |       .sort(([a], [b]) => a - b)
  145 |       .map(([s, c]) => `${s}=${c}`)
  146 |       .join(', ');
  147 | 
  148 |     expect(
  149 |       (tally.get(429) ?? 0) > 0,
  150 |       `red flag: 25 parallel requests to /api/storybooks/generate produced ZERO 429 (status mix: ${statusSummary}). Rate limiter not wired in — a single authenticated user can burst 25+ paid LLM + image-generation jobs in parallel.`,
  151 |     ).toBe(true);
  152 |   });
  153 | 
  154 |   test('ai-note/transcribe produces at least one 429 under a 25-request burst', async ({
  155 |     request,
  156 |   }) => {
  157 |     test.fail(
  158 |       true,
  159 |       'Risk Assessment finding #20 (gap coverage per ENG-2433): no rate limit on /api/ai-note/transcribe. ElevenLabs is per-minute billed — highest cost-abuse vector of the AI surface. Remove test.fail once rate limiting lands.',
  160 |     );
  161 | 
  162 |     // Empty body — route validates the storagePath / transcript ref
  163 |     // before any ElevenLabs job kickoff. No transcription spend.
  164 |     const tally = await fireBurst(() =>
  165 |       request.post('/api/ai-note/transcribe', {
  166 |         data: {},
  167 |         timeout: TIMEOUT_PER_REQ_MS,
  168 |       }),
  169 |     );
  170 | 
  171 |     const statusSummary = Array.from(tally.entries())
  172 |       .sort(([a], [b]) => a - b)
  173 |       .map(([s, c]) => `${s}=${c}`)
  174 |       .join(', ');
  175 | 
  176 |     expect(
  177 |       (tally.get(429) ?? 0) > 0,
  178 |       `red flag: 25 parallel requests to /api/ai-note/transcribe produced ZERO 429 (status mix: ${statusSummary}). ElevenLabs spend is per-minute — bursting this endpoint is the worst-case cost vector on the AI surface.`,
  179 |     ).toBe(true);
  180 |   });
  181 | 
  182 |   test('notes/[id]/generate produces at least one 429 under a 25-request burst', async ({
  183 |     request,
  184 |   }) => {
  185 |     test.fail(
  186 |       true,
  187 |       'Risk Assessment finding #20 (gap coverage per ENG-2433): no rate limit on /api/notes/[id]/generate. Reasoning-model LLM call per request. Remove test.fail once rate limiting lands.',
  188 |     );
  189 | 
  190 |     // Fake all-zeros UUID — route should 404 fast (note not found)
  191 |     // before any OpenAI call. No reasoning-model spend.
  192 |     const fakeNoteId = '00000000-0000-0000-0000-000000000000';
  193 |     const tally = await fireBurst(() =>
  194 |       request.post(`/api/notes/${fakeNoteId}/generate`, {
  195 |         data: {},
  196 |         timeout: TIMEOUT_PER_REQ_MS,
  197 |       }),
  198 |     );
  199 | 
  200 |     const statusSummary = Array.from(tally.entries())
  201 |       .sort(([a], [b]) => a - b)
  202 |       .map(([s, c]) => `${s}=${c}`)
  203 |       .join(', ');
  204 | 
  205 |     expect(
  206 |       (tally.get(429) ?? 0) > 0,
  207 |       `red flag: 25 parallel requests to /api/notes/[id]/generate produced ZERO 429 (status mix: ${statusSummary}). Reasoning-model calls are the most expensive per-request on the surface.`,
  208 |     ).toBe(true);
  209 |   });
  210 | });
  211 | 
```